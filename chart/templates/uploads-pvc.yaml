apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: billohip
    chart: billohip
  name: billohip-uploads
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 49Gi
  storageClassName: standard
